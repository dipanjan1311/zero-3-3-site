# Generated at build time by scripts/generate-manifests.js — never hand-edited,
# so there's no reason to track it or fight over it in merges.
assets/hero-gallery/manifest.json
assets/gallery/manifest.json
assets/live/manifest.json

# OS junk
.DS_Store
Thumbs.db
